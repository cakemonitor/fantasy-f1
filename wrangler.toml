name = "fantasy-f1"
compatibility_date = "2026-01-01"
pages_build_output_dir = "public"

[[kv_namespaces]]
binding = "F1_DATA"
id = "1b52376370bc41ef8ce405ed3aabab5f"
preview_id = "99f988bef7394a21a3becf05b12bebfd"

# ADMIN_PASSWORD is set via the Cloudflare dashboard (Secrets) — never commit it here.
# Cron is handled by the standalone Worker in cron-worker/ — deploy with: npm run deploy:cron
