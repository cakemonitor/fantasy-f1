name = "fantasy-f1-cron"
main = "worker.js"
compatibility_date = "2026-01-01"

[[kv_namespaces]]
binding = "F1_DATA"
id = "1b52376370bc41ef8ce405ed3aabab5f"
preview_id = "99f988bef7394a21a3becf05b12bebfd"

[triggers]
crons = ["*/10 * * * *"]

# ADMIN_PASSWORD is set via: npx wrangler secret put ADMIN_PASSWORD --config cron-worker/wrangler.toml
# Must match the Pages Worker's ADMIN_PASSWORD — the admin panel reuses one password end-to-end.
