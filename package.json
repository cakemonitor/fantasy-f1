{
  "name": "fantasy-f1-v2",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --kv F1_DATA",
    "seed": "node scripts/seed-dev-kv.js",
    "deploy:pages": "wrangler pages deploy public --project-name fantasy-f1 --commit-dirty=true",
    "deploy:cron": "wrangler deploy --config cron-worker/wrangler.toml",
    "cron:test": "wrangler dev cron-worker/worker.js --test-scheduled"
  },
  "devDependencies": {
    "wrangler": "^4.68.1"
  }
}
